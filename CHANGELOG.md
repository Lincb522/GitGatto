# 更新日志

GitGatto 的正式版本以 GitHub Release 为发布记录。应用内更新中心读取同一份发布说明并通过 GitHub Appcast 安装发布包。

## 未发布

## 0.18.18 — 2026-09-04

### 修复

- 项目页与“Git 与同步”设置会持续显示 GitHub 账号状态；未登录时可直接进入登录流程，已登录时显示当前账号。
- GitHub 登录可以正常打开终端完成授权，不再因发布包缺少系统自动化权限而失效。

## 0.18.17 — 2026-09-04

### 改进

- 项目语言标识改用 GitHub 开源生态中的原始 SVG 图标，常用语言在列表与详情页显示各自的专属标识。
- 图标按 Retina 比例从矢量资源渲染，并针对深浅色界面调整单色图标的对比度；未收录语言统一显示开源文件代码图标。

## 0.18.16 — 2026-09-03

### 改进

- 状态栏监控可以独立查看全部仓库或指定仓库，并汇总所选范围内的年度活动。
- 界面图标改用 Reicon 矢量资源，补齐灾备、找回丢失改动、发布就绪检查和状态栏监控等图标；统一 Retina 下的清晰度与显示尺寸。

### 修复

- 暂存时遇到遗留的 `.git/index.lock` 会先确认锁未被占用，再安全清理并重试；仍在使用或刚创建的锁保持不动，减少 `GG-GIT-STAGE-128`。
- 仓库保护基线被清理或迁移后会自动重建，不再把恢复点缺失误判为仓库损坏；并发检查只创建一份基线。
- 重复的监控配置与状态栏回调不再触发无效的工作区刷新，避免同步状态被取消错误覆盖。

## 0.18.15 — 2026-09-03

### 新增

- 新增统一监控状态：工作区、远程同步、仓库保护、GitHub Actions 与项目目标共用一套运行状态和异常提示。
- 新增状态栏监控窗口，可查看当前仓库、各监控通道的实时情况和最近一年的项目活动，并可直接打开主应用。
- 设置增加监控总开关、状态栏开关和五类监控通道的独立开关。
- 项目每日活动以点阵图展示提交次数和本地改动记录。

### 修复

- 仓库保护改为比较恢复基线之后新增的文件与行数变化，避免把原有未提交内容重复判定为异常，并在事件中列出实际触发阈值的路径。
- Agent 安装或升级开发工具后，必须通过可执行文件、版本命令和可升级状态检查才会显示成功；验证失败时保留原始输出并明确标记为未通过本机验证。

## 0.18.14 — 2026-09-03

### 新增

- 仓库守卫覆盖全部已管理的本地仓库，识别外部 Agent、终端、Git CLI 与脚本造成的仓库不可用、文件删除、未提交内容丢失以及分支、HEAD 和暂存区变化。
- 设置增加“Agent 写入保护”和“外部仓库保护”开关，可分别控制应用内 Agent 与外部工具的保护范围。
- GitGatto Agent 修改当前仓库前创建恢复点，完成后核对文件删除、内容覆盖和 Git 状态；发现风险时保留现场并进入检查状态。
- 灾备中心增加风险事件，可查看受影响路径、仓库状态和原始错误，并直接检查仓库或打开对应恢复点。

### 改进

- 恢复点采用原子写入和完整性标记；异常退出后忽略并清理未完成的恢复点。高风险事件会保留最近恢复点，避免现场被后续自动备份覆盖。
- 仓库变化监控增加根目录状态检查和定时校验，在文件事件延迟或丢失时仍能更新工作区与灾备状态。

### 修复

- Homebrew 操作的超时从进程实际启动后计算，不再把调度等待误判为执行超时。

## 0.18.13 — 2026-09-03

### 文档

- 仓库 README 增加简体中文、繁体中文、英语、日语、韩语、德语、法语、西班牙语、巴西葡萄牙语、俄语和阿拉伯语完整版本。
- 新增直接嵌入 README 与项目文档的路线图、系统架构图和 Star History。
- README 截图更新为当前应用界面，展示 GitHub 项目、工作区 Diff、灾备中心与文件时间机器。

### 修复

- 侧边栏收起后保留唯一的品牌标识：带独立品牌栏的主题隐藏栏内重复图标，轻雾主题继续显示单个应用图标。

## 0.18.12 — 2026-09-02

### 新增

- 新增灾备中心：实时监控已管理的本地仓库，定时保存未提交代码，重大改动即时生成恢复点，并可恢复到新的仓库副本。
- 新增备份管理：查看恢复点与空间占用，打开或删除单份备份、清理指定仓库或全部备份，并支持手动创建恢复点。

### 改进

- 每个仓库最多保留三份滚动恢复点；没有新改动或内容指纹相同时不重复备份。
- 备份目录可在设置中更换；迁移现有恢复点并核对内容后再切换到新位置。

### 修复

- Agent 安装统一使用受控写入目录；Homebrew 软件包由独立受控通道安装，其他 Agent CLI 也无法写出安装范围。
- GitHub CLI、云工具等软件的登录与令牌状态不再被误判为安装配置失败；本地可执行文件和无凭据配置完成后即标记为已安装。

## 0.18.11 — 2026-09-02

### 新增

- 项目目标增加完整发布流程，依次验证 README 与译文、版本号、构建号、更新日志、标签发布工作流、提交与推送、GitHub Release、DMG、Appcast 和本机应用版本。
- 发布准备可交给当前仓库的 Agent；版本标签推送与本机安装保留独立确认，GitHub Actions 与发行附件状态可在中断后继续跟踪。
- 项目目标支持自然语言自定义：Agent 生成可确认的候选条件，应用验证条件白名单、补齐依赖，并沿交付、Pull Request 或发布路径执行与核验。
- 开发工具中心扩充至 99 种工具，增加本地品牌图标、安装队列和升级队列。
- 开发工具支持三路并发操作；可多选、全选并批量升级，队列显示执行数量、等待顺序并支持单独取消。

### 改进

- Agent 安装或升级开发工具后会执行该工具要求的当前用户初始化、组件注册与配置迁移，将可执行文件目录持久化到登录 Shell 的 PATH，并重新验证配置、可执行文件与版本。

### 修复

- Homebrew 公式没有兼容 bottle 时允许正常源码构建，公式声明的构建依赖不再被误判为无关升级。
- Homebrew 升级改由 GitGatto 的受控通道执行，避免 Agent 沙箱与 Homebrew 构建沙箱嵌套导致 `sandbox-exec` 被 macOS 拒绝；Agent 继续完成升级后的用户配置与验证。
- 多路升级任务共享同一个 Homebrew 变更队列，避免公式及其依赖同时写入 Cellar 时触发包锁冲突。
- Docker Compose 通过 `~/.docker/cli-plugins` 完成当前用户注册，不读取或改写 Docker 凭据配置；配置未完成时保持“需要完成操作”并提供原操作重试。

## 0.18.10 — 2026-09-01

### 新增

- 应用仓库支持快捷安装：DMG 与 ZIP 使用本机安装流程，其他发行包由 Agent 检查、安装并显示阶段进度。
- 开发工具中心收录 63 种常用工具，可按分类检索、识别本机安装状态，并检查 Homebrew 可升级版本。
- 开发工具支持 Agent 安装与指定软件包升级，完成后重新检测可执行文件和版本。

### 改进

- 普通文本、Markdown 与 HTML 翻译的单次处理上限延长至 3 分钟；HTML 按批次独立计时，长文档不再共享同一个总超时时间。
- 开发工具扫描采用分批并发，并识别 Homebrew keg-only 路径，减少一次性启动大量版本检测进程造成的卡顿。
- Sparkle 更新组件升级至 2.9.6，更新失败时保留底层原因、错误域与错误代码。

### 修复

- 移除会强制重启 Sparkle 安装进度进程的旧兼容逻辑，避免下载完成后安装连接被中断。

## 0.18.9 — 2026-08-31

### 改进

- 全部界面语义图标更新为 GitGatto 专属图标，统一圆角单线结构、光学尺寸与状态语义。
- 图标资源针对 Retina 小尺寸显示重新生成，减少灰边和模糊，同时保持适中的线宽。
- README Agent 的空闲状态增加独立编写图标，应用仓库的网页入口使用统一图标组件。

## 0.18.8 — 2026-08-31

### 新增

- 新增“银页”主题：暖灰画布、石墨色窄导航轨、雾蓝状态卡片与低对比圆角面板采用独立布局，并适配深浅色外观。
- 新增“翠影”主题：深墨绿导航、明亮工作区与绿色状态层级采用独立布局，并完整适配深浅色外观。

### 改进

- 侧边栏各区域可以独立折叠，侧边栏可收为图标栏；项目列表与详情区域支持拖动调宽并记住尺寸。
- 关闭主窗口时可以选择最小化或退出，支持记住选择并在通用设置中随时修改；设置新增启动动画与自动更新选项。
- 启动动画期间并行预载工作区、当前项目与应用仓库，在内容就绪或达到等待上限后平滑进入主界面；减少动态效果时自动改为短暂淡入。
- 当前仓库会提前准备文件索引、首个改动与提交差异及诊断摘要；项目语言图标、应用 Logo 与开发者头像复用首屏缓存。
- 新安装默认使用轻毛玻璃主题，原默认主题更名为“轻雾”。
- README Agent 可直接处理尚未添加到本地的 GitHub 项目，运行中可从按钮取消；文件夹动效与侧边栏扫描入口按实际控件尺寸重新调整。
- Star 按钮保持完整标题与等高图标，更新状态沿按钮外框显示进度光带。
- AI 翻译沿用 README Agent 的按钮结构，以翻译图标和“正在翻译”状态显示进度；拉取与推送改用与克隆一致的滑动强调块、方向动效和轮廓进度。
- 拉取、推送、克隆和 Fork 并克隆只在任务运行时展开，核心操作完成后停止动效并恢复默认按钮；方向图标改为 1.8 秒连续淡入淡出轨迹。
- 调整侧边栏仓库添加入口，并提高 AI 翻译与 README 重写按钮未运行状态的图标清晰度和对比度。
- 应用仓库加载状态改为“检索应用仓库”。
- 实时 Git 状态只在数据变化时刷新界面，页面切换先完成首帧再加载详情；轻毛玻璃面板的阴影不再参与滚动内容合成。

### 修复

- 修复主窗口关闭代理在 Xcode 16 工具链下的 Swift 6 隔离编译错误。
- GitHub 搜索结果中的 README 翻译不再因 CLI 探测状态失效而静默禁用；点击后会执行翻译并显示实际错误。
- 同步状态始终按仓库数据显示；待推送使用警示色，后台刷新错误保留在诊断提示中。
- 修复轻毛玻璃主题选择卡片在较窄窗口内重叠的问题；下载完成标记使用白色勾号。

## 0.18.6 — 2026-08-30

### 改进

- 拉取与推送按钮默认显示为紧凑圆角矩形，悬停后横向展开显示文字，执行时沿完整外轮廓显示进度光带；克隆与 Fork 并克隆按钮的左侧强调色状态块会在悬停时展开，执行中显示状态与轮廓进度；翻译保留 Download Button 的分层滑动与完成反馈；错误处理中的 Agent 按钮采用扁平文字流光，README Agent 使用文件夹、纸张与铅笔动效。
- 应用仓库截图支持自动轮播与手动切换；README 原文、译文和重写预览支持卡片切换。
- Star 操作使用强调色按钮，合并 GitHub 标识、当前 Star 数与状态动画，悬停时显示一次横向高光；新增仓库、网络状态与 Agent 工具栏补齐对应的交互反馈。
- 应用仓库的详细介绍支持 Agent 翻译并缓存译文；截图只展示仓库实际提供的图片。
- 暂存与取消暂存会立即更新工作区，并只刷新实时 Git 状态；代码预览复用语法分析结果，媒体预览不再阻塞差异内容显示。
- 错误报告同时保留完整原始输出、错误代码、中文解释与对应处理建议，并覆盖 Git、Agent、GitHub、文件和网络常见错误。

### 修复

- 本地提交完成后明确显示尚未推送，推送成功后再切换为已同步状态。
- 修复 Octocat 加载动画眼睛不可见和浅色毛玻璃主题轮廓对比度不足的问题。

## 0.18.5 — 2026-08-30

### 新增

- 应用仓库详情增加独立的项目介绍与版本下载页面，展示项目 Logo、结构化介绍、功能、截图、开发者、版本与安装包信息。

### 改进

- 应用仓库搜索支持精确仓库名、开发者与模糊匹配，不再过滤低星项目；结果会在发行包探测完成时逐项显示，并缓存最近发行版。
- 本地仓库状态改为由文件变化触发并合并短时间内的连续更新，同时限制界面与语言图标缓存，降低空闲资源占用。

## 0.18.4 — 2026-08-30

### 改进

- 833 个语言图标改用针对 16、24、48 和 128 像素优化的圆角扁平资源，并按显示倍率选择对应栅格；没有匹配语言时使用同套占位图标。

## 0.18.3 — 2026-08-29

### 改进

- README 以实际仓库、GitHub 与 Agent 能力为主线，补齐运行范围、权限边界、技术栈和开源项目致谢。
- README 重写模板在删除重复解释的同时保留已实现能力、执行范围、前置条件、持久化与失败恢复信息。
- 关于界面、项目预览和应用仓库预览统一使用官网产品口号，并同步中英文文案。

## 0.18.2 — 2026-08-29

### 改进

- 关于窗口的官网入口现已连接 GitGatto 正式官网：[gatto.zijiu522.cn](https://gatto.zijiu522.cn)。

## 0.18.1 — 2026-08-29

### 新增

- GitHub 项目详情使用紧凑的主要语言技术标识，并沿用对应语言图标与 GitHub 语言色。

### 改进

- 重构项目 README 的内容层级与排版，补齐工作流、功能边界、技术栈标识、使用说明和开源项目致谢。
- 深化四套 README 重写模板，要求 Agent 从清单、锁文件、Imports 与现有素材中取证，并在预览前校验排版、技术版本、链接与图片。
- 语言技术标识会根据背景亮度自动选择前景色，改善浅色语言色上的文字可读性。

## 0.18.0 — 2026-08-29

### 新增

- 应用仓库详情与发布内容支持 Agent 翻译，并保留译文供后续直接切换。
- 工作区变更、提交历史和文件历史支持直接预览图片、视频与 SVG 内容。
- 项目、开发者和应用仓库搜索支持继续加载更多结果。

### 改进

- 全部界面语义图标统一为矢量资源，补齐普通与选中状态，并改善小尺寸清晰度。
- 为项目、GitHub、Agent、下载、更新和详情页面补齐非空白加载状态。
- GitHub 语言图标按显示尺寸进行高质量渲染，减少列表小图标的锯齿。
- 拉取、提交和推送进度直接显示在对应按钮中，移除重复的顶部同步动画。

### 修复

- 将项目 Agent 的执行上限从 150 秒延长到 15 分钟，避免构建、测试等长任务被过早终止，并保留手动取消操作。

## 0.17.0 — 2026-08-29

### 新增

- Agent 内置工作区、暂存审阅、提交起草、仓库诊断、同步、冲突、历史与 GitHub 检查技能。
- Git 错误报告可直接交给当前仓库的 Agent 诊断并执行本地修复。
- 提交信息起草在暂存区为空时自动暂存当前改动，并提供提交、提交并推送和重新起草操作。
- 增加可直接打开的共享 Xcode 工程、应用与测试 scheme，以及可重复生成的 XcodeGen 配置。
- 使用 imagegen 生成并接入 117 个圆角线条界面图标，覆盖应用内原有系统图标语义。
- 增加完整项目代码目录，支持源码、图片、视频和 SVG 预览，并可查看 SVG 源码。
- 增加 GitHub Releases、下载管理、macOS 应用安装和支持平台筛选的应用仓库。
- 支持仓库 Star、本地仓库添加与删除，以及 Agent 编辑和美化 README。
- 搜索支持项目、开发者、模糊匹配与自然语言检索。
- 接入 833 个 GitHub 语言图片图标。

### 改进

- Agent 持续显示当前仓库、分支及暂存状态，并扩展对 Git LFS、子模块、worktree、钩子与签名问题的诊断范围。
- 调整关于窗口的垂直位置，使顶部与底部留白保持对称。
- 重构代码、差异与冲突界面，完善文件操作和项目详情交互。
- 优化项目切换与 Git 状态刷新，次要数据改为后台补全。

## 0.16.2 — 2026-08-28

### 改进

- 本地仓库切换先显示核心状态，再在后台补全未跟踪文件、历史、贮藏与工作树。
- 合并分支、同步状态与工作区变更读取，减少持续刷新产生的 Git 进程。
- 最近访问的仓库保留内存快照，重复切换时立即恢复界面并后台校准。

### 修复

- 修复主窗口和关于窗口的窗口控制按钮留白。
- 修复默认主题设置页品牌区域偏右的问题。

## 0.16.1 — 2026-08-28

### 新增

- 项目详情概览支持手动收起，并在阅读文档时自动切换为紧凑状态。
- 更新按钮增加检查、发现更新与完成检查的状态动画，并适配减少动态效果。
- 增加完整版本日志界面，可查看全部 GitHub 发布版本及对应说明。

### 改进

- 轻毛玻璃和控制台主题为窗口控制按钮保留统一空间。
- 关于窗口采用紧凑布局，窗口控制按钮与标题区域融为一体。
- 检查更新直接开始执行，不再额外打开更新中心。
- 更新中心优先展示最新版本日志，并可进入完整版本历史。
- 控制台主题底部工具栏增加“关于”入口。

### 修复

- 修复已是最新版本时被显示为检查失败的问题。
- 修复轻毛玻璃主题关于界面文字方向异常的问题。
- 修复关于窗口恢复旧尺寸后出现过多上下留白的问题。

## 0.15.0 — 2026-08-28

### 新增

- 增加支持浅色与深色外观的控制台主题，使用独立命令顶栏、功能轨道、底部仓库坞和设置状态侧栏。
- 增加就绪与运行状态呼吸灯，并在“减少动态效果”开启时保持静态。
- 增加工作树、贮藏、文件时间线、仓库诊断与冲突解决工作区。
- 增加 GitHub Actions 中心与 Pull Request 审查工作区。

### 改进

- 重构代码、差异和冲突结果界面，统一行号、语法层级与操作工具栏。
- 项目列表与详情显示仓库主要语言颜色，并在图标资源缺失时使用统一代码标识。
- 完善 Git 状态刷新、远端同步和错误诊断流程。

## 0.14.2 — 2026-08-27

### 改进

- 默认主题的设置窗口改为固定分类侧栏、页面工具栏与连续设置工作区。
- 轻毛玻璃主题使用独立浮动顶栏、横向分类坞和分组玻璃面板。
- 设置页移除重复的解释性副标题，并完善最小窗口、深色外观与英文界面的排版。

## 0.14.0 — 2026-08-27

### 新增

- 接入 GitHub 项目、开发者搜索、每日推荐、Fork、克隆、Pull Request 和项目详情。
- 接入真实本地 Git 工作区、暂存、提交、历史、分支、Pull 与 Push。
- 增加项目 Agent、提交信息起草、Pull Request 回复与文档翻译。
- 增加 GitHub Releases 更新通道、应用内更新中心和更新日志。
- 增加默认专业主题、轻毛玻璃主题、浅深色外观和强调色设置。

### 改进

- 项目扫描改为手动启动、后台执行、按权限筛选并由用户选择添加。
- 项目文档支持 Markdown、图片、仓库内链接、目录与代码浏览。
- 项目 Agent 与翻译使用独立 CLI 通道并保存操作记录。
- 全局错误界面保留完整 Git 输出、退出代码和脱敏诊断信息。

### 修复

- 恢复默认主题并保留轻毛玻璃为独立选项。
- 修复品牌图标和字标的小尺寸锯齿、缝隙与对齐问题。
