# 移动端与远程连接开发文件

当前已暂停开发。这里保留未接入正式应用的后端与 Swift 通信包，不属于当前发布版本。

- `Backend/relay/`：TypeScript / Node.js 后端，包括源码、测试、依赖锁与容器配置。
- `Packages/GitGattoRemote/`：Swift 身份、加密和通信客户端骨架，尚未编译验证或接入应用。
- `ci/relay.yml`：测试工作流草稿，不在 `.github/workflows/` 中，不会自动运行。恢复开发时先审阅，再决定是否移回。

开发范围与暂停状态见 [路线图](../../docs/ROADMAP.md#移动端与远程连接已暂停)。基础后端曾通过本地测试，但后续接口改动和 Swift 骨架尚未验证，不能沿用此前结果作为当前版本通过的证明。

本次仅移动现有文件并调整路径引用，未继续开发、构建、部署或发布。
