# Product

## Register

product

## Users

个人开发者与软件团队。他们需要在日常开发中快速打开本地仓库，理解工作区状态，审阅差异，组织暂存内容，提交变更，与远端同步，并借助已登录的本机 CLI Agent 完成仓库任务。

## Product Purpose

提供一款 macOS 原生 Git 与 GitHub 客户端。核心成功标准是让“发现项目 → Fork 或克隆 → 查看改动 → 暂存 → 提交 → 拉取或推送 → 处理 PR”成为清晰、可信且高效的连续工作流。

## Brand Personality

精致、专业、自信。界面应让复杂版本控制操作显得有秩序，但不淡化操作的真实结果。

## Anti-references

不采用未经设计的系统默认控件堆叠，不模仿终端界面，不使用花哨的开发者仪表盘、装饰性玻璃卡片或大面积高饱和色。避免为了视觉差异而重造标准交互。

## Design Principles

- 让变更及其影响成为界面主角。
- 常用操作直接可达，危险操作必须明确表达结果。
- 用一致的状态语言区分已暂存、未暂存、同步和错误。
- 暂存、工作区与上游跟踪状态持续更新；Agent 执行不得阻塞状态监测或翻译执行通道。
- 本地项目扫描只由用户手动启动，可在后台继续，并在专用界面中选择要添加的可管理仓库；依赖、构建、缓存和系统目录不进入结果。
- 保持高信息密度，但通过层级与间距维持可扫描性。
- 平台实现可以不同，核心 Git 行为必须一致。
- Agent 默认只读；修改权限必须由用户在当前任务中明确选择，并保持在当前仓库边界内。
- 项目操作与翻译使用独立、可分别配置的 CLI 执行通道，任何支持参数或标准输入提示词的 CLI 都可通过自定义模板接入。
- GitHub 搜索、推荐和 PR 数据必须来自当前 API 响应；Fork、评论等远端写入只在用户明确触发后执行。
- GitHub 账号项目从现有本机凭据加载；登录由 GitHub CLI 标准流程处理，令牌不进入应用界面。
- Agent 结果使用对话式表达，翻译只返回译文，不显示文档化标题标记。
- Agent 以 Git/GitHub 仓库维护为专业边界，内置补丁、历史、分支、冲突、Pull Request 与 Actions 诊断能力；远端写入仍由明确的应用操作控制。
- Agent 起草的提交信息只有在用户点击“提交并推送”后才触发真实 Git 写入；项目文档使用 GitHub 返回的完整 Markdown HTML，并保存对话、操作记录和译文。
- 暂存区内可直接起草并填入提交信息；简洁与完整格式由应用设置持久化。
- 设置必须覆盖应用通用偏好、Git 与同步、Agent 和翻译，而不是只暴露 CLI 配置。
- 外观设置保留默认专业主题，并把轻毛玻璃作为独立可选主题；切换主题同时改变窗口材质、工作区布局、分栏和控件样式。
- 菜单、帮助中心和关于窗口遵循同一套中英文术语；帮助内容必须覆盖所有已交付工作流和可恢复错误。
- GitHub 搜索覆盖项目与用户/开发者，用户结果按用户名的精确、前缀、包含和编辑距离排序。
- 应用内更新以 `Lincb522/GitGatto` 的 GitHub Releases 为唯一版本、更新日志和安装包来源；Appcast 与附件必须通过 HTTPS 匿名读取。
- 关于窗口直接显示版本、构建号、检查更新、开发者、官网与开源入口；用户协议、开源协议、隐私政策和免责声明在独立的应用内阅读界面中查看。

## Accessibility & Inclusion

支持浅色与深色主题、中英文界面、键盘导航、清晰焦点状态和不依赖颜色的变更标识。文本与背景遵循 WCAG AA 对比度要求。
