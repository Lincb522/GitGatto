## 新增

- 设置新增“退出后继续监控”，开启后由独立后台助手在主程序退出后继续监控仓库、记录活动，并按已有保护设置创建恢复点。
- 后台状态栏可查看全部仓库或单个仓库，沿用主程序的主题；主程序打开时，监控和状态栏仍正常工作。
- “在状态栏显示”和“退出后继续监控”分别设置。后台监控默认关闭，需要系统批准时可从设置进入授权页面。

## 修复

- 修复外部命令提前结束后，继续向其发送输入可能导致应用崩溃的问题；命令失败时保留错误输出。
