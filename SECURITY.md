# 安全策略

## 报告问题

请通过开源仓库的私密安全报告渠道提交漏洞。公开地址启用前，不要在公开 Issue 中附带令牌、私钥、私有仓库内容或可直接利用的敏感数据。

报告应包含受影响版本、可复现步骤、预期与实际结果，以及已脱敏的日志或最小样例。

## 安全边界

- GitGatto 复用 Git、SSH、GitHub CLI 与 Agent CLI 的正常凭据来源，不读取或复制凭据值。
- 更新包必须通过 HTTPS 获取并通过 Sparkle EdDSA 签名验证。
- 更新签名私钥不得进入仓库、构建日志、应用包或 CI 明文变量输出。
- GitHub 评论、Fork、Push、合并等远端写入只能由明确操作触发。
- Agent 默认只读；允许修改时仍限制在当前仓库。

维护者确认报告后会给出受影响范围与修复计划。尚未验证的判断不会作为安全公告发布。
