# Design System

## Direction

深夜代码审阅桌：中性石墨工作面、清晰的文本层级，以及只在关键操作和状态上出现的余烬橙。整体采用克制型配色，视觉服务于仓库内容。

## Color

颜色以 OKLCH 定义，并在运行时转换为 sRGB。

### Light

- Background: `oklch(1.000 0.000 0)`
- Sidebar: `oklch(0.960 0.002 255)`
- Raised surface: `oklch(0.985 0.002 49)`
- Ink: `oklch(0.180 0.015 49)`
- Muted ink: `oklch(0.470 0.018 49)`
- Primary: `oklch(0.620 0.180 49)`
- Accent: `oklch(0.410 0.125 221)`

### Dark

- Background: `oklch(0.225 0.004 255)`
- Sidebar: `oklch(0.265 0.004 255)`
- Raised surface: `oklch(0.295 0.004 255)`
- Ink: `oklch(0.945 0.006 49)`
- Muted ink: `oklch(0.690 0.012 49)`
- Primary: `oklch(0.720 0.170 49)`
- Accent: `oklch(0.760 0.105 200)`

## Typography

使用系统字体。界面标签保持紧凑，仓库名和页面标题使用适度加重；提交哈希、分支名、路径和 Diff 使用等宽设计。正文不使用展示字体。

## Layout

macOS 首版采用三层工作区：仓库与功能导航、当前集合、内容检查器。窗口变窄时缩减导航和集合宽度，内容检查器保持弹性。最小窗口尺寸为 960 × 620。

## Components

- 导航项使用低对比背景表达选中状态，主色仅用于当前状态标记。
- 按钮分为主操作、次操作和无边框工具操作；同一主题内的相同行为在各界面保持一致。
- 变更行同时显示状态字母、文件名和路径，不单独依赖颜色。
- Diff 行保留行号列，新增、删除和区块标题具有不同的背景与前景色。
- 成功结果使用短暂的窗口内通知；失败结果使用持续显示的错误详情窗口，完整输出不限行并可滚动选择，同时显示稳定应用错误代码、Git 退出代码或系统错误代码、操作、仓库与安全命令。诊断信息可复制，潜在凭据在进入界面前脱敏。
- 顶部工具栏使用文字与图标同时表达已同步、待推送、待拉取和未配置上游；工作区文件右键菜单提供与当前文件直接相关的 Git 和系统操作。
- 顶部当前分支使用可展开的紧凑菜单；当前项带选中标识，选择其他本地分支后由真实 `git switch` 执行并刷新完整仓库状态。
- 关于窗口使用品牌标记、产品定位、版本、构建号、更新入口、开发者、官网与开源入口构成单层信息结构，不展示平台或 CLI 版本。
- 更新中心使用单独窗口呈现当前版本、构建号、稳定通道、自动检查、自动下载和检查结果；安装交互继续使用 Sparkle 标准更新界面。
- 协议阅读器使用固定侧栏切换用户协议、开源协议、隐私政策与免责声明，正文保留可选择文本和复制全文操作。
- Agent 工作区使用持续可见的运行模式、进度和停止操作；用户消息与结果保持可选择文本，不模拟终端输出。
- Agent 标题区持续显示当前本地项目与暂存数量，暂存区快捷操作只使用该项目刚刚读取的暂存 Diff。
- Agent 结果采用连续对话排版，标题标记不会作为原始 `###` 文本显示；提交信息结果在同一条回复下提供“提交并推送”和“重新起草”，命令、文件改动、运行模式和完成时间在对应回复下可展开并随对话持久保存。
- 设置窗口的轻雾主题使用横向图标分类栏和连续表单；轻毛玻璃使用左侧分类与半透明分组；控制台使用紧凑文本索引与连续表单。三种布局共享同一设置状态，Agent 与翻译拥有独立配置和进程。
- “Git 与同步”提供本地状态与只读上游状态的独立开关和间隔；监测状态在窗口内直接显示。
- 暂存区提交编辑器支持多行内容，Agent 起草操作与当前简洁/完整格式状态紧邻，并将结果直接填入编辑器。
- 帮助中心采用固定主题导航与连续文章布局，覆盖每个真实工作流、快捷键和恢复路径；菜单栏所有顶级菜单与新增命令使用同一套本地化资源。
- 项目工作区是应用默认入口，独立占满内容区域，保持“项目列表 → 完整项目详情”的双栏结构；概览完整渲染 GitHub Markdown 文档，代码和 Pull Request 使用原生管理界面。
- 项目搜索在项目与用户/开发者之间切换；用户结果列表显示头像、账号和类型，详情显示公开资料与项目，并保持双栏布局。
- README 将文档链接解析为可路由的项目地址、将仓库图片解析到原始资源地址；图片与视频限制在正文宽度内，私有仓库资源以内嵌内容显示。
- README 的仓库文档与代码链接在项目详情中原位导航，外部网页使用应用内浏览器；文档工具栏提供返回、AI 翻译和持久译文切换。
- README 翻译只向已配置的翻译 AI 分批提交可见文本节点，HTML 标签、属性、代码和资源地址不进入翻译输入，返回后按原位置重建文档；整个任务共享 90 秒截止时间，译文按仓库、路径、目标语言和原文内容寻址保存。
- 应用图标使用 1024 × 1024 坐标网格上的透明纯路径 SVG 猫形 Git 环；猫耳与环形轮廓连续衔接。浅色界面读取深色矢量，深色界面读取浅色矢量，不在 Swift 中重新绘制。ICNS 的 16–1024 px 表示分别从矢量源按目标像素渲染，避免逐级缩放产生的软边和锯齿。
- GitGatto 标题使用独立的纯路径 SVG 字标，连接的 `tt` 暗示分支路径，末尾单个珊瑚色节点形成识别点；字标不重复猫头、猫耳或 AppIcon 轮廓。浅色界面使用深色矢量字标，深色界面使用浅色矢量字标，在任意显示缩放下保持清晰边缘。
- 侧栏品牌区避开窗口控制键，放大的图标与标题作为一个整体靠左排列并垂直居中；侧栏收起后保留一个品牌图标，同时显示展开控件与功能轨道。
- 侧栏导航与本地项目目录位于同一连续滚动区域，品牌栏与底部外观、设置入口固定可达；只展示当前用户可读写并可管理的有效 Git 仓库，按“近期活跃、最近修改、更早”分区，当前项目与最近打开记录优先，各区按 Git 活动及文件修改时间倒序排列。标题同时显示项目总数与扫描状态，刷新按钮在扫描期间替换为进度指示。

## Motion

状态变化使用 160–220ms 的平滑动画。实时状态监测、Agent 运行、Pull、Push 和提交后推送使用循环动效表达未完成状态。“曜幕”主题的两组环境光分别以 11 秒和 13 秒缓慢位移，不参与内容转场；开启“减少动态效果”后全部保持静态。列表选择、侧栏切换和通知进入只表达状态，不安排页面加载编舞。

## Themes and Localization

浅色与深色配色保留相同的信息层级，主题不改变功能与数据。所有主题的导航均可滚动、展开与收起，仓库列表和内容区域沿用可拖动分栏。收起后保留一个品牌图标；品牌与窗口控制键纵向错开，不向右挤占仓库工具栏。

- **轻雾**：不透明的中性灰白与柔和石墨色，连续源列表与无间隙工作面。导航不加图标底板，38pt 行高；6pt 圆角控件、细分隔线。设置页用图标与文字上下排列的横向分类栏，分组标题与表单横向对齐。
- **轻毛玻璃**：AppKit `behindWindow` 材质，通透侧栏与单块磨砂内容面板。仓库上下文和同步操作合并在内容面板顶端，不再堆叠浮动坞；面板用16pt圆角和短投影。设置页左侧分类、右侧半透明分组。减少透明度时面板使用实色。
- **控制台**：紧凑编辑器工作台。68pt品牌与仓库操作顶栏，左侧54pt工具轨和可收起的仓库目录；右侧是可横向滚动的工作区标签、紧凑文件列表和差异编辑区。差异标题压为38pt文件标签，底边展示真实路径、分支与同步状态。4pt控件与等宽目录文字；设置页为紧凑文本索引和连续表单。
- **翠影**：贯穿窗口的深森林绿侧栏，浅色工作面为纯白。内容上方以当前功能标题和仓库上下文分两行组织，操作靠右；工作区更改以差异阅读区为主体，右侧为可调宽度的文件与提交检查器。不套独立卡片，使用连续分隔；44pt导航行。设置页沿用深色分类栏与连续表单。
- **银页**：哑光银灰底与独立纸面。侧栏与银灰底连为一体，使用带文字的分组导航和单层选中面；仓库目录从侧栏入口展开，收起后保留无底板图标列。顶部是品牌、当前功能与仓库上下文；更改文件横向排列，下方为差异纸页及可调宽度的独立提交面板。文件仍保留暂存切换、状态文字和完整右键操作。设置页为实色分类标签与纸面表单。

“曜幕”参考 StarHub 的珊瑚与紫色环境光，使用 AppKit `behindWindow` 材质。94pt 贯穿顶栏容纳品牌、仓库、分支和同步操作，给窗口控制键保留上方空间；导航直接贴合窗口左边，右侧仅保留一层内容工作面，不重复功能标题与统计。选中导航使用浅浮层与细边界，图标不加方块底。设置窗口采用贯穿品牌栏、可横向滚动的分类标签和连续分组；关于窗口采用连续排版与细分隔线。浅色为暖白玻璃，深色为近黑玻璃，两种外观均保留低饱和环境光。减少透明度时使用不透明底色，减少动态效果时环境光保持静止。

主题同时控制窗口材质、布局、面板层级、圆角、间距和控件样式；强调色只用于主操作、当前选择和焦点状态。所有用户可见文本从本地化资源读取；首版包含英文和简体中文。
